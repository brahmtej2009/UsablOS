# ─── UsablOS Environment ───────────────────────────────────────────────

# Port everything will run on in production
PORT=5174

# Production flag
NODE_ENV=production

# JWT Secret — change this to a long random string in production!
JWT_SECRET=your_super_secret_jwt_key_change_me_in_prod
